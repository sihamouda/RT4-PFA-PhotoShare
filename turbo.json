{
  "$schema": "https://turborepo.org/schema.json",
  "globalDotEnv": [".dev.env"],
  "pipeline": {
    "build":{"cache": false},
    "dev": {
      "cache": false,
      "dotEnv": [".dev.env"],
      "dependsOn": ["^build"],
      "persistent": true,
      "inputs": ["src/**"]
    }
  }
}
