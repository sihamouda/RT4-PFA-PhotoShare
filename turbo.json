{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build":{"cache": false},
    "dev": {
      "cache": false,
      "dotEnv": [".dev.env"],
      "dependsOn": ["^build"],
      "persistent": true,
      "inputs": ["src/**"]
    }
  }
}
