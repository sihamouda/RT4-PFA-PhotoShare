{
  "devDependencies": {
    "turbo": "^1.13.0",
    "dotenv-cli": "^7.4.1"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -e .dev.env -- turbo run dev"
  },
  "dependencies": {
    "@nestjs/config": "^3.2.0",
    "@nestjs/typeorm": "^10.0.2",
    "joi": "^17.12.2",
    "pg": "^8.11.3",
    "typeorm": "^0.3.20"
  }
}
